# OpenProject API Documentation Lookup

Look up OpenProject API v3 documentation for: $ARGUMENTS

Search for:
1. **Endpoint details** - HTTP method, path, parameters
2. **Request format** - Required/optional fields, data types
3. **Response structure** - Expected response schema
4. **Examples** - Sample requests and responses
5. **Permissions** - Required user permissions
6. **Filters** - Available filter operators and syntax

Common endpoints:
- `/api/v3/projects` - Project management
- `/api/v3/work_packages` - Work package CRUD
- `/api/v3/time_entries` - Time tracking
- `/api/v3/memberships` - User/project memberships
- `/api/v3/relations` - Work package relations
- `/api/v3/versions` - Milestones/versions

Use web search or Context7 to find latest documentation.
Return structured summary with examples.
